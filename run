#!/bin/sh

install() {
	go get -t ./...
}

build() {
	go build \
	  -ldflags "-X 'main.BinaryVersion=$1' -X 'main.BinaryBuildHash=$2'" \
	  -o ./out/klog \
	  klog.go
}

test() {
	go test ./...
}

format() {
	go fmt ./...
}

cli() {
	go run klog.go "$@"
}

# MAIN
task=$1
shift 1
$task "$@"
